import { Injectable } from '@angular/core';
import { items } from '../interfaces/items';

@Injectable({
  providedIn: 'root'
})
export class FilteredService {
  filteredItems: items[] = [];

  constructor() { }

  filt
}
